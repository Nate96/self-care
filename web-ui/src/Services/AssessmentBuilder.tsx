import {Assessment, Category, Question, Response } from "../lib/types"
import FormApi from "./FormApi"

function buildAssessment(id: string): Assessment { }

function buildEmptyAssment(): Assessment {}

export default { buildAssessment }
  
